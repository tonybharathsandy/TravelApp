/* eslint-disable no-unused-vars */
import hotelData from '../../data/hotels.json'

let initialData = hotelData;

function hotelsData(state = initialData, action){
        return state
} 

export default hotelsData